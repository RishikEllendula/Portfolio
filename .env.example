# No environment variables are required to run this project as-is.
#
# If you wire up the contact form (see app/api/contact/route.ts) to a real
# email provider, add its credentials here, e.g.:
# RESEND_API_KEY=
